<div>
  <h1>DashBoard ADMIN</h1>
  <p>Hola, {{user.name}}</p>

  <p>En este DashBoard podés gestionar un CRUD completo de productos.</p>

  <h2>Agregar nuevo producto</h2>
  <form id="productForm">
    <input type="text" name="title" placeholder="Título" required />
    <input type="text" name="description" placeholder="Descripción" />
    <input type="text" name="code" placeholder="Código" required />
    <input type="number" name="price" placeholder="Precio" required />
    <input type="number" name="stock" placeholder="Stock" required />
    <input type="text" name="category" placeholder="Categoría" required />
    <input type="text" name="thumbnail" placeholder="URL imagen" />
    <button type="submit">Agregar producto</button>
  </form>

  <br>

  <h2>Lista de productos</h2>
  <table>
    <thead>
      <tr>
        <th>Título</th>
        <th>Código</th>
        <th>Precio</th>
        <th>Stock</th>
        <th>Categoría</th>
        <th>Fecha de Creación</th> <!-- 🔥 -->
        <th>Última Modificación</th> <!-- 🔥 -->
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody id="productTable">
      {{#each products}}
        <tr>
          <td>{{this.title}}</td>
          <td>{{this.code}}</td>
          <td>${{this.price}}</td>
          <td>{{this.stock}}</td>
          <td>{{this.category}}</td>
          <td>{{this.createdAt}}</td> <!-- 🔥 -->
          <td>{{this.updatedAt}}</td> <!-- 🔥 -->
          <td>
            <button class="delete-btn" data-id="{{this._id}}">Eliminar</button>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <div style="margin-top: 20px; text-align: center;">
    {{#if pagination.hasPrevPage}}
      <a href="/users/dashboard-admin?page={{pagination.prevPage}}&limit=10">
        ◀️ Anterior
      </a>
    {{/if}}

    <span> Página {{pagination.page}} de {{pagination.totalPages}} </span>

    {{#if pagination.hasNextPage}}
      <a href="/users/dashboard-admin?page={{pagination.nextPage}}&limit=10">
        Siguiente ▶️
      </a>
    {{/if}}
  </div>

  <br>

  <div>
    <p>Volver al perfil: <a href="/users">Ingresar</a></p>
    <p><a href="/api/sessions/logout">Cerrar sesión</a></p>
  </div>
</div>

<script src="/js/admin.js"></script>
